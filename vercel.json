{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "build": {
    "env": {
      "CI": "false"
    },
    "commands": {
      "build": "npm install --legacy-peer-deps && npm run build"
    }
  }
}
